defaults: &defaults
  adapter: sqlite3
  timeout: 1000

production:
  <<: *defaults
  database: dukejour

development:
  <<: *defaults
  database: db/dukejour_dev.sqlite3

test:
  <<: *defaults
  database: dukejour_test
